const num1 = 5;

const num2 = 10;

const addValues = () => {
  console.log(`the sum is: ${num1 + num2}`);
};

addValues();

// in the app.js, if you require, this will run, even if you didnt invoke at app.js

// app.js
// require("./04-alternative-flavor")
